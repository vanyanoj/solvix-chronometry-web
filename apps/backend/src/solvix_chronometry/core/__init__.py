"""Компилируемое ядро Solvix Chronometry.

Здесь живёт ноу-хау: детекторы аномалий, аналитические расчёты,
хелперы событий. В прод-сборке пакет компилируется Cython в .so —
исходники в поставку клиенту не попадают.

Правило: новая «думающая» логика (нормативы, приоритеты, статистика)
добавляется сюда, а не в api/ или watchdog/.
См. Обсидиан → «Защита и лицензирование».
"""
